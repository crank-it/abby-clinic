# ============================================================================
# YOONET BRAND .cursorrules
# Drop this file into any project root for on-brand Yoonet builds
# ============================================================================

# SECTION 1: CORE RULES
# ============================================================================

## Stack
- Framework: Next.js 14+ (App Router)
- Styling: Tailwind CSS ONLY
- Components: shadcn/ui
- Database: Supabase
- Deployment: Vercel

## Critical Rules

### Styling (Non-negotiable)
1. TAILWIND ONLY — No custom CSS files, no CSS modules, no inline style={{}}
2. USE shadcn/ui — Don't recreate buttons, inputs, cards, dialogs, etc.
3. USE cn() HELPER — For conditional classes
4. USE CSS VARIABLES — bg-background, text-foreground, text-primary, etc.

### File Structure
```
app/
├── (marketing)/          # Public pages
├── (dashboard)/          # Protected pages
├── api/                  # API routes
├── globals.css           # Tailwind + CSS variables ONLY
└── layout.tsx            # Root layout
components/
├── ui/                   # shadcn/ui (don't edit these)
└── [feature]/            # Feature components
lib/
├── supabase/             # Supabase client
└── utils.ts              # cn() helper
```

# ============================================================================
# SECTION 2: YOONET BRAND DESIGN SYSTEM
# ============================================================================

## Typography

### Fonts
| Purpose | Font | Usage |
|---------|------|-------|
| Headings & Buttons | Dosis | Bold, friendly, rounded |
| Body Text | Lato | Clean, readable |

### Font Setup (layout.tsx)

```tsx
import { Dosis, Lato } from "next/font/google"

const dosis = Dosis({ 
  subsets: ["latin"],
  variable: "--font-heading",
  weight: ["400", "500", "600", "700"]
})

const lato = Lato({ 
  subsets: ["latin"],
  variable: "--font-body",
  weight: ["400", "700"]
})

// In <body>:
<body className={`${dosis.variable} ${lato.variable} font-body antialiased`}>
```

### Tailwind Config

```ts
// tailwind.config.ts - add to theme.extend
fontFamily: {
  heading: ["var(--font-heading)", "sans-serif"],
  body: ["var(--font-body)", "sans-serif"],
},
boxShadow: {
  'yoonet-sm': '0 6px 18px rgba(31,41,55,0.08)',
  'yoonet-md': '0 10px 30px rgba(31,41,55,0.12)',
},
```

### Typography Classes

| Element | Classes |
|---------|---------|
| Brand title (H1) | `font-heading text-3xl font-bold text-primary` |
| Section title (H2) | `font-heading text-2xl font-bold text-foreground` |
| Subtitle | `text-[15px] text-foreground` |
| Question/Label (bold) | `text-[17px] font-bold text-foreground` |
| Body text | `text-[17px] text-foreground` |
| Small/muted | `text-sm text-muted-foreground` |
| Button text | `font-heading font-bold` |

---

## Colour Palette

### CSS Variables (globals.css)

```css
@layer base {
  :root {
    --background: 230 25% 97%;        /* #F6F7FB */
    --foreground: 230 16% 50%;        /* #6b7094 */
    
    --card: 0 0% 100%;                /* #FFFFFF */
    --card-foreground: 230 16% 50%;
    
    --primary: 230 100% 77%;          /* #8A9EFF - brand blue */
    --primary-foreground: 0 0% 100%;
    
    --secondary: 230 33% 93%;         /* #E3E8EF */
    --secondary-foreground: 230 16% 50%;
    
    --muted: 230 25% 97%;
    --muted-foreground: 226 10% 65%;  /* #9BA1B4 */
    
    --accent: 343 94% 68%;            /* #FA6492 - pink */
    --accent-foreground: 0 0% 100%;
    
    --destructive: 0 84% 60%;
    --destructive-foreground: 0 0% 100%;
    
    --border: 228 25% 92%;            /* #E6E8EF */
    --input: 228 25% 92%;
    --ring: 230 100% 77%;
    
    --radius: 1rem;
  }
}
```

### Colour Reference

| Colour | Hex | Usage |
|--------|-----|-------|
| Brand Blue | #8A9EFF | Primary buttons, links, CTAs, brand titles |
| Pink Accent | #FA6492 | Progress bars, alerts, highlights |
| Text | #6b7094 | Main body text |
| Muted | #9BA1B4 | Secondary text, placeholders |
| Background | #F6F7FB | Page backgrounds |
| Card | #FFFFFF | Cards, panels |
| Border | #E6E8EF | Borders, dividers |

### Yoonet Background Gradient

```tsx
// For marketing pages
<div className="min-h-screen bg-gradient-to-br from-[#f0f2f8] via-[#fafafc] to-[#f5f6fb] bg-fixed">
```

### Progress Bar

```tsx
<div className="h-2 rounded-full bg-border overflow-hidden">
  <div 
    className="h-full rounded-full bg-gradient-to-r from-primary to-accent transition-all duration-500"
    style={{ width: `${progress}%` }}
  />
</div>
```

---

## Components

### Install Command

```bash
npx shadcn@latest add button card input textarea form label select dialog sheet skeleton sonner tabs avatar badge progress
```

### Yoonet Card

```tsx
<Card className="border-border bg-card shadow-yoonet-md rounded-2xl">
  <CardHeader>
    <CardTitle className="font-heading text-2xl font-bold">Title</CardTitle>
  </CardHeader>
  <CardContent>{/* Content */}</CardContent>
</Card>
```

### Yoonet Button

```tsx
// Primary
<Button className="bg-primary hover:bg-primary/90 font-heading font-bold rounded-xl transition-all hover:-translate-y-0.5 hover:shadow-yoonet-md">

// Secondary
<Button variant="secondary" className="font-heading font-bold rounded-xl">
```

### Yoonet Input

```tsx
<Input className="rounded-xl border-border focus:border-primary" />
```

---

## Brand Assets

### Logo
```tsx
<img 
  src="https://s3.amazonaws.com/appforest_uf/f1679452072099x521288658380311800/yoonet-bubble-app-square-logo.svg"
  alt="Yoonet"
  className="h-10 w-auto"
/>
```

---

## Yoonet Style Rules

### DO ✅
- Use Dosis for headings/buttons, Lato for body
- Use rounded corners: `rounded-2xl` (cards), `rounded-xl` (buttons/inputs)
- Use brand blue (#8A9EFF) for primary actions
- Use pink accent (#FA6492) for progress/highlights
- Add hover lift: `hover:-translate-y-0.5 hover:shadow-yoonet-md`
- Keep max-width: `max-w-3xl` (800px)

### DON'T ❌
- Don't use sharp corners
- Don't use green for CTAs
- Don't use fonts other than Dosis/Lato
- Don't skip hover states

# ============================================================================
# SECTION 3: PAGES & CONTENT
# ============================================================================

## Page Structure

### Server Component Page

```tsx
import { Metadata } from "next"

export const metadata: Metadata = {
  title: "Page Title | Yoonet",
  description: "Description",
}

export default async function Page() {
  return (
    <div className="container py-8 space-y-8">
      {/* Content */}
    </div>
  )
}
```

### Loading State

```tsx
import { Skeleton } from "@/components/ui/skeleton"

export default function Loading() {
  return (
    <div className="container py-8 space-y-8">
      <Skeleton className="h-10 w-48" />
      <div className="grid gap-4 md:grid-cols-2 lg:grid-cols-3">
        {Array.from({ length: 6 }).map((_, i) => (
          <Skeleton key={i} className="h-32 rounded-2xl" />
        ))}
      </div>
    </div>
  )
}
```

# ============================================================================
# SECTION 4: LAYOUTS
# ============================================================================

## Marketing Layout

```tsx
export default function MarketingLayout({ children }: { children: React.ReactNode }) {
  return (
    <div className="flex min-h-screen flex-col bg-gradient-to-br from-[#f0f2f8] via-[#fafafc] to-[#f5f6fb] bg-fixed">
      <Header />
      <main className="flex-1">{children}</main>
      <Footer />
    </div>
  )
}
```

## Dashboard Layout

```tsx
export default function DashboardLayout({ children }: { children: React.ReactNode }) {
  return (
    <div className="min-h-screen flex bg-background">
      <Sidebar className="hidden lg:flex lg:w-64 lg:flex-col lg:fixed lg:inset-y-0" />
      <main className="flex-1 lg:pl-64 py-6">{children}</main>
    </div>
  )
}
```

# ============================================================================
# SECTION 5: FORMS
# ============================================================================

## Form Pattern

```tsx
"use client"

import { zodResolver } from "@hookform/resolvers/zod"
import { useForm } from "react-hook-form"
import { z } from "zod"
import { Button } from "@/components/ui/button"
import { Form, FormControl, FormField, FormItem, FormLabel, FormMessage } from "@/components/ui/form"
import { Input } from "@/components/ui/input"

const schema = z.object({
  name: z.string().min(2),
  email: z.string().email(),
})

export function ContactForm() {
  const form = useForm<z.infer<typeof schema>>({
    resolver: zodResolver(schema),
    defaultValues: { name: "", email: "" },
  })

  return (
    <Form {...form}>
      <form onSubmit={form.handleSubmit(onSubmit)} className="space-y-6">
        <FormField
          control={form.control}
          name="name"
          render={({ field }) => (
            <FormItem>
              <FormLabel className="text-[17px] font-bold text-foreground">Name</FormLabel>
              <FormControl>
                <Input className="rounded-xl" {...field} />
              </FormControl>
              <FormMessage />
            </FormItem>
          )}
        />
        <Button 
          type="submit" 
          className="bg-primary font-heading font-bold rounded-xl hover:-translate-y-0.5 transition-all"
        >
          Submit
        </Button>
      </form>
    </Form>
  )
}
```

# ============================================================================
# SECTION 6: COMPONENTS
# ============================================================================

## Always Use shadcn/ui For:
- Buttons, Cards, Inputs, Forms, Dialogs, Dropdowns, Tables, Tabs

## Icons
```tsx
import { Plus, Loader2 } from "lucide-react"
<Plus className="h-4 w-4" />
```

# ============================================================================
# END OF RULES
# ============================================================================
